# Phase G handoff

Truth surface: `/Users/liusihang/.codex/worktrees/d790/openwebui` on `codex/pr7-chat-agent-dual-mode-20260726`, starting from `531ae10b9`. Phase G integrates sanitized mode-profile defaults into new conversation state and model changes. Administrator UI, backend, Docker, remote, and live are out of scope.

## Required behavior

- Both modes continue to use the existing system default-model selection; profiles never select a model.
- Apply sanitized profile defaults exactly once when a new persistent or temporary draft is initialized.
- Explicit profile values override model metadata; explicit empty/null clears model defaults; omitted fields inherit model defaults.
- Filter all values against current model support, global gates, resources, and user permissions; show safe warnings.
- Users may temporarily adjust Terminal/tools/skills/filters/features per conversation.
- Switching models retains valid temporary choices and removes only incompatible/unauthorized choices; it never reapplies profile defaults or changes the bound revision.
- Reasoning Depth remains on its existing path and is not changed by mode profiles.
- Preserve the public revision hint in draft/request state until backend canonical binding is returned; never expose administrator Prompt in frontend state.
- Terminal state must not leak between conversations, modes, resets, or model changes.

## Checkpoints

- [x] G0 Verify HEAD, frontend baseline, and current initialization/reset/model-change seams.
- [x] G1 Add pure resolver RED tests.
- [x] G2 Implement resolver and warning contract.
- [x] G3 Integrate one-time new-chat/temporary initialization and revision hint.
- [x] G4 Integrate model-change retention and Terminal leakage fixes.
- [x] G5 Focused frontend tests, real compile checks, formatting, and scope audit.
- [ ] G6 Independent spec/quality review and remediation.
- [x] G7 Commit verified Phase G files; no push.

## Evidence log

- 2026-07-27: created before Phase G edits. Existing modified Phase E handoff and untracked deploy/Phase D handoff remain outside this phase.
- 2026-07-27 G0: verified `HEAD=531ae10b9` on the assigned branch. Focused existing non-watch frontend baseline passed 6 files / 50 tests: conversation mode presentation/compiler, Agent request shaping, default features, history sync, and system terminal tools. The relevant seam is `Chat.svelte`: `resetInput()` currently clears selections and re-runs `setDefaults()` on a model change, while `selectedTerminalId` is a global store. Public sanitized profiles arrive as `$config.conversation_mode_profiles`; no frontend Profile Prompt field exists.
- 2026-07-27: resumed at `531ae10b9`; `git status` shows only handoff/deploy artifacts, with no Phase G business-code edits. The prior truncated spawn left no waitable agent state, so it will not be treated as implementation evidence.
- 2026-07-27: reviewed the approved Phase G plan and confirmed no design blocker. Execution will use test-first implementation followed by independent specification and quality reviews.
- 2026-07-27 G1 RED: `npm run test:frontend -- src/lib/components/chat/conversationModeProfiles.test.ts --run` failed as expected because a model-change selection retained both `profile-terminal` and `code_interpreter`.
- 2026-07-27 G3 RED: `npm run test:frontend -- src/lib/components/chat/ConversationMode.presentation.test.ts --run` failed as expected before Chat imported the resolver and emitted the revision hint.
- 2026-07-27 G5 GREEN: focused non-watch Vitest passed 6 files / 42 tests: resolver, mode presentation/compiler, request shaping, default features, and system terminal tools. Direct `svelte/compiler` compilation of `Chat.svelte` passed. `prettier --write` touched Phase G files without further changes and `git diff --check` passed. Touched-file `svelte-check` filtering still reports pre-existing broad `Chat.svelte` TypeScript diagnostics (implicit-any and incomplete store metadata types); no parser/compile failure occurred.
- 2026-07-27 G5 files: `src/lib/components/chat/Chat.svelte`, `src/lib/components/chat/conversationModeProfiles.ts`, `src/lib/components/chat/conversationModeProfiles.test.ts`, `src/lib/components/chat/ConversationMode.presentation.test.ts`, and this handoff.
- 2026-07-27 G6: implementer self-review confirmed no backend/admin/Docker/remote/live changes, no frontend Administrator Prompt field, no profile model selection, and no reasoning-depth mutation. Independent specification review is now running; independent quality review has not started.
- 2026-07-27 G7: committed the core Phase G frontend/tests/handoff files as `fe109611f` (`feat(chat): apply mode-specific capability defaults`), then committed the persistent-binding follow-up as `d00fdacfb` (`fix(chat): preserve existing mode profile bindings`); no push performed.
- 2026-07-27 G6 follow-up review: reconciled a concurrent Phase G commit (`fe109611f`) with the assigned worktree. Verified the resolver's tri-state/default/filter/model-change contract, draft controller's one-time and canonical-hint behavior, request hint path, no Prompt/model/reasoning fields, and Terminal/Code Interpreter exclusion. The only validated follow-up defect was that an existing persistent chat could reach profile initialization through `setDefaults()` after loading; initialization is now guarded to new `draft:` state only. Preserved unrelated Phase E/F handoffs and deploy/Phase D artifacts untouched.
- 2026-07-27 G6 controller note: the initial truncated spawn was later discovered to have continued as a second worker and produced `d00fdacfb`. Because the first independent spec review targeted only `fe109611f`, it was stopped before verdict. Both commits will be reviewed together from `531ae10b9..d00fdacfb`.
- 2026-07-27 G5 controller verification: reran the combined focused frontend gate at `d00fdacfb`; 8 Vitest files / 66 tests passed, including resolver, mode presentation/compiler, request shaping, defaults, Terminal tools, history synchronization, and composer model settings.
- 2026-07-27 G6 independent spec review: not compliant. Validated required fixes are direct-Terminal permission filtering; preventing existing bound chats without a saved draft from emitting current model/profile overrides; preserving a restored draft revision hint through model changes; and aligning frontend model-support filtering with backend semantics (`function_calling === false` disables Tools/Skills/Filters/Terminal, Terminal also honors `terminal === false`, feature capability is unsupported only when explicitly false).
- 2026-07-27 remediation RED: resolver test failed for omitted feature capability support, missing direct-tool permission helper, and missing controller draft-hint hydration; Chat source integration test failed for missing direct-Terminal/immutable-bound request guards.
- 2026-07-27 remediation implementation: direct Terminal availability, model defaulting, `terminal_id`, and direct `tool_servers` now use TerminalMenu-compatible permission semantics. Bound chats without drafts omit controlled capability fields and retain voice/memory; explicit composer changes opt into request overrides. Resolver now honors `function_calling === false`, explicit-false feature support, and restored-hint hydration.
- 2026-07-27 remediation GREEN: focused non-watch 8-file Vitest gate passed 70 tests (the prior 66 plus four new remediation cases); direct `Chat.svelte` compilation, Prettier check, and `git diff --check` passed.
- 2026-07-27 remediation audit: G6 remains open. Root-cause tracing showed `MessageInput.svelte` invokes `onChange` reactively for prompt/file changes as well as capability changes. The remediation currently marks any such event as a capability override, so merely typing in an existing bound chat can still send empty/false controlled overrides and replace bound defaults. A capability-only change detector and failing regression are required before spec re-review.
- 2026-07-27 capability-tracker RED: pure tests failed for the missing capability-only tracker across prompt/file/reasoning-only updates and six controlled capability fields; the Chat integration source test failed because both `MessageInput` callbacks still set the override flag directly.
- 2026-07-27 capability-tracker implementation: added a sticky capability fingerprint that ignores prompt/files/reasoning, establishes a bound-chat baseline, opts in on tool/skill/filter/web-search/code-interpreter/image-generation changes, and remains explicit after returning to empty. Terminal remains independently tracked through `selectedTerminalId`.
- 2026-07-27 capability-tracker GREEN: focused 8-file non-watch gate passed 78 tests; direct `Chat.svelte` compilation, Prettier check, and `git diff --check` passed. G6 remains unchecked pending independent specification and quality reviews.
- 2026-07-27 spec re-review: not compliant. Prompt-only drafts still became explicit capability overrides after reload; explicit clears serialized as omission instead of `[]`/`null`; revoked `direct_tool_servers` could still emit selected direct tool servers; initial `/` draft restoration did not hydrate its revision hint; and existing unbound/legacy chats still reinitialized model defaults.
- 2026-07-27 architecture correction: the repeated defects share one root cause—booleans inferred capability authority from draft existence instead of representing `new initialized`, `existing inherit-bound`, and `explicit user override` states. The next remediation must use one explicit state contract, persist it in drafts, serialize inherit as omission and explicit clears as `[]`/`null`, and apply direct-tool permission filtering to every request path.
- 2026-07-27 execution error: the resumed original implementer remained running for multiple full wait windows without file/test progress or blocker output. It was closed before any new write and replaced by one fresh no-context Terra worker; no parallel writer remains.
- 2026-07-27 final state-model remediation RED: the reviewer tests failed as intended before production changes—15 pure authority/serialization cases failed because the explicit controller and serializer were absent, and 4 Chat source-contract cases failed because the prior implementation still inferred authority from draft/bound booleans.
- 2026-07-27 final state-model remediation GREEN: replaced the boolean tracker with a pure `initialized`/`inherit_bound`/`explicit` authority controller plus a centralized request serializer. Existing chats now default inherit-bound; new drafts initialize authoritatively; only real controlled changes (including Terminal) latch explicit. Both composer draft saves persist the marker; restored drafts sanitize direct server selections; root drafts hydrate revision hints before model change; denied direct-server paths cannot restore or emit direct tools/terminals.
- 2026-07-27 verification checkpoint: the focused 8-file frontend gate passed 88 tests (the current suite includes the new reviewer cases); direct `Chat.svelte` compiler invocation and Prettier write completed successfully; `git diff --check` passed. G6 remains unchecked pending independent specification and quality reviews.
- 2026-07-27 third spec re-review: not compliant. Remaining Important fixes are to suppress direct Tool Server emission when `function_calling === false`; treat malformed root drafts and stale `inherit_bound` markers as new-chat initialization rather than inheritance; and emit only the currently selected direct Terminal server so cleared state from a previous chat cannot leak.
- 2026-07-27 fourth spec re-review: not compliant. A structurally valid pre-Phase-G root draft without an authority marker still suppresses capability initialization and restores empty controls as explicit clears; and authorized `direct_server:*` selections are missing from model-change availability, so valid direct tools are removed on model switch.
- 2026-07-27 fifth spec re-review: not compliant. Existing-chat canonical revision can be overwritten by a stale draft hint; Terminal is missing from draft snapshots; restored explicit snapshots are not immediately revalidated after permission/resource drift; and an authority marker is trusted even when no complete capability snapshot exists.
- 2026-07-27 sixth spec re-review: not compliant. Critical: unlocked Chat/Agent switching changes only the mode label and leaks the prior profile revision/capabilities/Terminal. Important: the composer can autosave before async initialization completes, and restored direct Tool/system Terminal selections can be removed before their external catalogs finish loading.
- 2026-07-27 seventh spec re-review: not compliant. Send-time revalidation mutates global capability arrays after the submit path clears the draft, recreating a stale root draft; continuation/regeneration also validates against the globally selected model rather than the actual per-call model argument.
- 2026-07-27 eighth spec re-review: not compliant. Automatic submit entry points bypass the two UI-local `clearDraft()` calls, so an accepted auto-submission can leave the old root capability snapshot for the next new chat.
- 2026-07-27 ninth spec re-review: not compliant. The Cmd/Ctrl+Shift+Enter Generate Message Pair path is a separate accepted-input creator and still bypasses both `submitHandler` and draft clearing.
- 2026-07-27 tenth spec re-review: not compliant. Clearing the draft without suppressing the input-reset `onChange` flush can recreate the just-cleared root snapshot; the same asynchronous ordering risk applies to ordinary accepted submissions.
- 2026-07-27 eleventh spec re-review at `50a2040be`: `Spec compliant`. Independent code-quality review is now the remaining G6 gate.
- 2026-07-27 independent quality review: not approved. Important findings: uncached/unbounded external discovery blocks every send; request-local state is read after awaits instead of frozen before them; catalog failures replace truth with empty state; positive legacy draft capability choices are silently discarded; and OAuth-required profile tools are filtered instead of entering the existing pending-auth flow.
- 2026-07-27 final timing guard: existing-chat authority is now restored from the draft marker (or set inherit-bound) before `loadChat()` can trigger reactive model handling, preventing legacy/unbound chats from reaching controlled defaults during load. The same 8-file gate remained green after this correction. G6 remains unchecked pending independent specification and quality reviews.
- 2026-07-27 final narrow remediation RED: pure profile tests failed four validated reproductions—missing validated draft parsing, stale root `inherit_bound` restoration, direct server IDs surviving `function_calling === false`, and missing selective tool-server assembly. Chat source-contract tests failed for truthiness-based root draft suppression and unconditional direct server/Terminal assembly.
- 2026-07-27 final narrow remediation implementation: added a pure stored-draft parser, coerced new-chat `inherit_bound` markers to `initialized`, disabled `toolServerIds`/emission when function calling is unsupported, and centralized the optional `tool_servers` property. Supported initialized/explicit requests can still send `tool_servers: []`; inherit/unsupported requests omit the property. Direct Terminal emission now matches only the serialized `terminal_id`, so a cleared selection cannot leak stale no-ID terminal entries.
- 2026-07-27 final narrow remediation GREEN: focused 8-file frontend gate passed 91 tests (88 prior plus 3 new cases); direct `Chat.svelte` compilation, Prettier check, and `git diff --check` passed. G6 remains unchecked pending independent specification and quality reviews.
- 2026-07-27 legacy/direct-availability RED: two pure tests failed for the missing legacy-root authority classifier and missing IntegrationsMenu-compatible direct-tool availability builder; two Chat source-contract cases failed before those helpers were integrated.
- 2026-07-27 legacy/direct-availability implementation: valid pre-Phase-G root drafts now restore prompt, files, reasoning, and an explicitly stored mode while current profile/model capabilities still initialize normally. Only root markers `initialized` or `explicit` suppress initialization and restore controlled fields. Model-change availability now includes live permitted `direct_server:<index>` IDs only for `$toolServers` entries with `server.info`; denied and absent IDs continue to be filtered.
- 2026-07-27 legacy/direct-availability GREEN: focused 8-file frontend gate passed 94 tests (91 prior plus 3 new cases); direct `Chat.svelte` compilation, Prettier check, and `git diff --check` passed. G6 remains unchecked pending independent specification and quality reviews.
- 2026-07-27 complete-snapshot remediation RED: `conversationModeProfiles.test.ts` ran 32 tests with 2 failures because no complete typed draft-snapshot validator/hydrator existed. `ConversationMode.presentation.test.ts` ran 13 tests with 4 failures covering stale existing-chat revision hydration, missing Terminal persistence in all three draft writers, and missing authoritative snapshot revalidation before request emission.
- 2026-07-27 complete-snapshot remediation implementation: capability markers are now accepted only with typed tool/skill/filter arrays, all three feature booleans, and a string-or-null Terminal selection; existing chats accept only complete `explicit` snapshots. Existing chat loading preserves backend-canonical revisions and ordinary legacy prompt/files/reasoning while authoritative snapshots load catalogs and pass through the model-change resolver before controls become ready or requests can emit. All draft save paths now persist `selectedTerminalId`; permission/resource/model drift clears invalid restored selections and uses the existing warning-once path.
- 2026-07-27 complete-snapshot remediation GREEN: the focused 8-file frontend gate passed 97 tests (94 prior plus 3 new cases). Direct `svelte/compiler` compilation of `Chat.svelte`, Prettier check, and `git diff --check` passed. The only formatter output was the repository's existing ignored `pluginSearchDirs` warning. G6 remains unchecked pending independent specification and quality reviews.
- 2026-07-27 profile-boundary/readiness remediation RED: the pure suite ran 33 tests with 2 expected failures for missing snapshot-version validation and configured direct-tool fallback; the presentation suite ran 16 tests with 3 expected failures for assignment-only mode switching, pre-initialization authoritative autosave, and destructive revalidation before external catalogs. Follow-up RED checks also caught raw multi-model defaulting and the missing authority rebase for resolver-owned changes.
- 2026-07-27 profile-boundary/readiness remediation implementation: unlocked pre-send mode selection now runs one awaited profile-boundary transition that preserves the selected model and ordinary draft state while replacing revision, capabilities, Terminal, warning, draft, and authority state before initializing the destination profile once. Draft authority now requires snapshot version `1`; autosaves omit authority/version until controls, Terminal, and revision are finalized, then an immediate root-or-chat-targeted save persists the final snapshot. External direct-tool/direct-Terminal/system-Terminal truth is awaited before destructive filtering and request emission, with permission-gated configured direct IDs used only while live truth is unavailable. Resolver-owned changes rebase the authority fingerprint without becoming user overrides.
- 2026-07-27 profile-boundary/readiness remediation GREEN: focused 8-file frontend gate passed 102 tests (97 prior plus 5 new cases); direct `Chat.svelte` compilation, Prettier check, and `git diff --check` passed. The only formatter output was the existing ignored `pluginSearchDirs` warning. G6 remains unchecked pending independent specification and quality reviews.
- 2026-07-27 send-local remediation RED: `conversationModeProfiles.test.ts` ran 35 tests with 1 expected failure because the request-local capability resolver did not exist. `ConversationMode.presentation.test.ts` ran 18 tests with 2 expected failures because `sendMessageSocket(model)` still used the mutating interactive revalidator/global selected model and both first-send handlers cleared drafts without awaiting completion or suppressing post-clear root autosave.
- 2026-07-27 send-local remediation implementation: request assembly now resolves copied effective capabilities against the actual socket model and loaded catalogs, then serializes those local values without mutating UI selections, authority/controller/revision, controls readiness, or draft state. Interactive model-change and restored-snapshot revalidation remain on their existing mutating path; inherit-bound requests still omit controlled fields. Both first-send handlers now await draft clearing and submission, and root autosave is suppressed once the first user message exists while the backend chat ID is pending.
- 2026-07-27 send-local remediation GREEN: focused 8-file frontend gate passed 105 tests (102 prior plus 3 new cases); direct `svelte/compiler` compilation of `Chat.svelte`, Prettier check, and `git diff --check` passed. The only formatter output was the existing ignored `pluginSearchDirs` warning. G6 remains unchecked pending independent specification and quality reviews.
- 2026-07-27 accepted-submit clearing RED: `ConversationMode.presentation.test.ts` ran 19 tests with 2 expected failures because every awaited `clearDraft` remained in the two visible composer wrappers and `submitHandler` had none. The passing portions enumerated suggestions, same-origin/confirmed postMessage, desktop/URL, Call Overlay, and both composer caller paths; the failing assertions isolated accepted queue/normal-send clearing and validation/web-search-confirmation ordering.
- 2026-07-27 accepted-submit clearing implementation: both accepted `submitHandler` outcomes now await `clearDraft($chatId || null)` exactly once. Queue acceptance records the item before clearing its draft and UI input; normal send clears only after OAuth/input/model/upload/count/confirmation/previous-error validation. Initial web-search confirmation and every rejected input retain their draft. The visible composers no longer clear locally, so suggestions, postMessage, desktop/URL, Call Overlay, and both composers share the same boundary. Request-local capability validation remains unchanged.
- 2026-07-27 accepted-submit clearing GREEN: focused 8-file frontend gate passed 106 tests (105 prior plus 1 net new case after replacing the prior composer-only assertion with two centralized-caller/ordering regressions); direct `svelte/compiler` compilation of `Chat.svelte`, Prettier check, and `git diff --check` passed. The only formatter output was the existing ignored `pluginSearchDirs` warning. G6 remains unchecked pending independent specification and quality reviews.
- 2026-07-27 message-pair shortcut RED: `ConversationMode.presentation.test.ts` ran 20 tests with 1 expected failure because the Cmd/Ctrl+Shift+Enter `createMessagePair` path contained no awaited draft clear. The test found both shortcut bindings and both invalid model-selection branches, then failed only on the missing clear between successful model lookup and history/chat mutation.
- 2026-07-27 message-pair shortcut implementation: after selected-model validation succeeds, `createMessagePair` now awaits `clearDraft($chatId || null)` before reading or mutating history and before creating/saving a chat. Empty model selection and missing catalog models preserve the root/chat draft and existing shortcut/UI behavior is otherwise unchanged.
- 2026-07-27 message-pair shortcut GREEN: focused 8-file frontend gate passed 107 tests (106 prior plus 1 new shortcut ordering case); direct `svelte/compiler` compilation of `Chat.svelte`, Prettier check, and `git diff --check` passed. The only formatter output was the existing ignored `pluginSearchDirs` warning. G6 remains unchecked pending independent specification and quality reviews.
- 2026-07-27 accepted-reset critical-section RED: `ConversationMode.presentation.test.ts` ran 21 tests with 4 expected failures: the queue and normal accepted branches had zero critical-section calls, `createMessagePair` had zero, and no suppression helper existed to hold draft persistence through the reset-triggered `onChange` flush. The other 17 presentation contracts remained green.
- 2026-07-27 accepted-reset critical-section implementation: queue acceptance, normal accepted send, and `createMessagePair` now use one short helper that suppresses draft persistence, clears the correct root/chat key, awaits input/prompt/files reset as appropriate, awaits a final Svelte `tick`, and releases suppression in `finally` before submit/model/history work continues. `saveDraft`, finalized/authority snapshot writers, and both composer `onChange` snapshot paths honor suppression. Invalid inputs and initial web-search confirmation never enter the critical section.
- 2026-07-27 accepted-reset critical-section GREEN: focused 8-file frontend gate passed 108 tests (107 prior plus 1 new reset-race contract); direct `svelte/compiler` compilation of `Chat.svelte`, Prettier check, and `git diff --check` passed. The only formatter output was the existing ignored `pluginSearchDirs` warning. G6 remains unchecked pending independent specification and quality reviews.
- 2026-07-27 independent-quality remediation RED: `conversationModeProfiles.test.ts` ran 40 tests with 5 expected failures for absent catalog fingerprint/cache, immutable request capture, legacy-positive migration, and OAuth partition interfaces. `ConversationMode.presentation.test.ts` ran 23 tests with 4 expected wiring failures for bounded background discovery, capture-before-await serialization, root/existing legacy restore, and global OAuth partitioning. Existing tests outside those new contracts remained green. Baseline targeted diagnostics also confirmed three pre-existing test-only TypeScript errors at dynamic draft deletion and prompt/file/reasoning fixture inputs.
- 2026-07-27 interrupted-remediation checkpoint: the implementation worker errored under provider high demand after leaving an uncommitted 4-code-file remediation diff. The main thread preserved it at HEAD `50a2040be`, confirmed the requested reasoning UI commits are already ancestors, and requested an independent read-only quality audit before any commit/build.
- 2026-07-27 resumed-gate RED: 8 focused files ran 115 tests; 111 passed and 4 `ConversationMode.presentation` contracts failed. Three failures reference old source names/layout after the catalog refactor; the fourth suggests existing-chat positive legacy draft migration may not be invoked. No production edit follows until root-cause tracing distinguishes stale contracts from real behavior gaps.
- 2026-07-27 interrupted-refactor root cause: the new resolver/cache/helper layer was present, but `sendMessageSocket` still called the old resolver shape, referenced deleted `modeProfileExternalCatalog`, read mode/revision/authority after awaits, and neither root nor existing-chat restore invoked positive legacy migration. Targeted `svelte-check` confirmed the undefined symbol/signature errors.
- 2026-07-27 quality-remediation GREEN: request context and reasoning are captured before the first await; serialization uses only captured mode/revision/authority/model/features/direct-server state plus the request catalog view; background external discovery is fingerprinted, deduplicated, bounded, retryable after error, and retains last success; legacy positive choices are wired for root/existing drafts; OAuth-required tools enter pending auth. Focused 8-file gate passed 115 tests, direct Svelte compile/Prettier/diff checks passed, and the newly touched catalog/type/test ranges have zero targeted diagnostics.
- 2026-07-27 review-follow-up RED/GREEN: source and API tests failed because send still awaited `modeProfileSetupPromise` and `getTerminalServers` still folded discovery errors into `[]`. Send now proceeds from its pre-await snapshot without waiting for setup/external discovery; the terminal API has an opt-in strict mode used only by catalog refresh, so failure calls cache `fail()` and preserves last-known truth while legacy callers retain `[]` fallback. Focused gate is 9 files/117 tests; expanded terminal/chat/Agent frontend is 19 files/255 tests; related backend is 163 tests.
- 2026-07-27 production build: `npm run build` reused Pyodide cache, transformed 6374 modules, and wrote the static adapter `build` output successfully. Only existing repository-wide Svelte warnings were emitted; tracked status remained scoped.
- 2026-07-27 final spec follow-up: independent review found three Important edge cases in the uncommitted quality remediation: positive legacy capabilities on an existing revision inherited unrelated defaults from the current administrator revision; OAuth-required tools disappeared from model-change/draft snapshots; and a successful unchanged-fingerprint external catalog never refreshed, so removed resources could remain indefinitely.
- 2026-07-27 edge-case RED: the two pure/source-contract suites failed nine new assertions for partial override ownership, changed-observation reporting, OAuth pending persistence/request exclusion, and catalog refresh age/retry behavior.
- 2026-07-27 edge-case implementation: migrated existing-chat legacy drafts now carry a typed partial `overrideFields` marker and serialize only historically positive fields, leaving omitted fields for the backend-bound revision. Any real composer/Terminal/feature edit promotes the snapshot to full explicit. Pending OAuth IDs participate in UI/model-change/draft state but are excluded from the outgoing request until authorization. Catalog entries track attempt/success times, refresh successful entries after 60 seconds in the background, retry failures after 10 seconds, retain last-known truth, and revalidate controls only when a refreshed catalog materially changes.
- 2026-07-27 edge-case GREEN: focused pure/presentation gate passed 65/65. Direct `Chat.svelte` compilation, Prettier check for all touched frontend/API files, and `git diff --check` passed. The wider focused/extended regressions and final independent reviews remain pending before commit/build.
- 2026-07-27 lifecycle review RED/GREEN: a new source-contract test failed before production changes because background catalog completion had no conversation/component generation guard. Navigation, new-draft/mode transitions, and component cleanup now invalidate a monotonic generation; background completion captures it, checks it before scheduling, and guarded revalidation checks again around every await before mutating UI/draft authority. Focused 9-file gate passed 120/120, direct Svelte compile and diff check passed.
- 2026-07-27 deep lifecycle/permission RED: final review reproduced three continuation gaps: guarded background revalidation could still initiate an old OAuth redirect after its internal `tick`; delayed `setDefaults()` could write after navigation because it was outside the generation contract; and permission-revoked direct Terminal URLs survived a failed first catalog load because classification data had been erased together with discovery credentials.
- 2026-07-27 deep lifecycle/permission GREEN: expected generation now propagates through model/profile application and is rechecked immediately before OAuth redirect. `setDefaults()` tracks the generation of its in-flight promise, serializes cross-generation callers, and checks freshness after catalog and OAuth waits before writes. Direct Terminal IDs remain classification-only when permission is denied, direct discovery stays disabled, and fallback candidate filtering removes configured direct URLs while preserving system Terminal IDs. Request usage capability also reads the frozen request model. Focused 9-file gate passed 121/121; expanded 19-file frontend gate passed 259/259; direct Svelte compile and diff check passed.
- 2026-07-27 parent-generation/deleted-terminal RED: spec re-review showed that an invalidated `setDefaults()` could return into an old `initNewChat()` that continued clearing the new chat, and a deleted historical direct Terminal URL was no longer present in settings for classification. New regressions failed both paths, including request serialization with an empty configured-direct set.
- 2026-07-27 parent-generation/deleted-terminal GREEN: `initNewChat()` captures the generation created by `beginModeProfileDraft()` and checks it after every yielding operation plus deferred call/focus callbacks before later state mutation. A shared pure direct-Terminal classifier treats HTTP(S) IDs as direct independently of current settings; availability filtering and request serialization both use it, so deleted/revoked URLs cannot survive or send while system Terminal IDs remain valid. Focused 9-file gate remains 121/121; direct compile/diff checks pass. Final expanded rerun and independent reviews remain pending.
- 2026-07-27 socket completion/queue ownership RED: a new presentation contract failed against completion, task-cancel, sidebar refresh, queued submission, and manual stop paths because they did not carry the originating chat generation across awaits.
- 2026-07-27 socket completion/queue ownership GREEN: completion and queue handlers now require the originating chat ID plus catalog generation, reject stale work before global writes/submission, and stage the sidebar list before ownership-checked publication. The first focused run exposed a real Svelte scoped-store compile error caused by a shadowing `chatId` parameter; renaming it to `targetChatId` fixed the root cause. Focused 9-file gate now passes 125/125.
- 2026-07-27 final local verification: the broader 23-file terminal/chat/Agent/API frontend regression passed 286/286, Prettier and `git diff --check` passed, and `npm run build` completed in 1m01s after transforming 6374 modules and writing the static `build` output. Build output contains existing repository Svelte warnings only; no generated tracked file was added.
- 2026-07-27 Phase G exit review: independent final specification review returned `Spec compliant`; independent final code-quality review returned `Approved` with no Critical or Important findings. The exact reviewed tree is now eligible for the scoped commit; remote image construction still waits for that committed-source checkpoint.
- 2026-07-27 nested-restore RED/GREEN: review showed parent post-await checks were insufficient because typed/legacy restore and final draft persistence mutate internally. Expected generation now propagates through `navigateHandler`, model change, root restore, legacy migration, mode transition, finalization, and `persistFinalizedModeProfileDraftSnapshot`; each helper checks before writes and after its own waits. Stale `runModeProfileSetup` callbacks exit before state mutation, and mode-transition loading cleanup is generation-owned. Focused 9-file gate passed 121/121; direct compile/diff checks pass.
- 2026-07-27 controls-autosave RED/GREEN: an in-flight controls autosave could return after navigation and bind chat A's canonical revision into chat B's global draft controller. `saveControls()` now freezes target chat ID and generation before the request, still lets the backend persist the originating chat, and ignores the response for global `chat`/revision state unless both ownership checks still match. New source contract passed; focused gate is 9 files/122 tests with direct compile/diff green.
- 2026-07-27 chat-load ordering RED/GREEN: overlapping navigation loads could complete out of order because `loadChat()` wrote its API response into globals before the parent generation check. `loadChat(targetChatId, expectedGeneration)` now keeps chat/tags responses local, verifies active chat ID and generation after each network/tick boundary, and only then commits global chat/mode/model/history/canonical revision. Navigation, socket event reload, and reconnect reload pass captured ownership. Focused gate is 9 files/123 tests with direct compile/diff green.
- 2026-07-27 deferred/socket/action RED/GREEN: quality review found an unguarded existing-chat authority microtask plus async `chat:tags` and `chatAction` responses that could write into a later chat. The microtask now verifies generation; tag refresh stages chat/tag results and verifies event chat/generation after each await; chat actions freeze target chat/generation, reject stale success/error responses before history writes, and stage save/sidebar responses before global assignment. Related title/embed/execute post-await callbacks now share the same event ownership checks. Focused gate is 9 files/124 tests with direct compile/diff green.
